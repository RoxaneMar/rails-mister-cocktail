<%= simple_form_for [@cocktail, dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description, placeholder: "What quantity of this ingredient do we need?" %>
  <%= f.submit "Add new dose", class: 'btn btn-primary' %>
<% end %>

