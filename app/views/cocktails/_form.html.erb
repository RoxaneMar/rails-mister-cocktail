<%= simple_form_for cocktail do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.submit "Add a new cocktail", class: 'btn-medium' %>
  <%= link_to 'Back', cocktails_path, class:'btn-medium' %>
<% end %>
