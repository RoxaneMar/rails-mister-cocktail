<%= simple_form_for cocktail do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.submit "Edit cocktail", class: 'btn-medium' %>
  <%= link_to 'Back', @cocktail, class: 'btn-medium'%>
<% end %>
